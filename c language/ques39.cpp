#include<bits/stdc++.h>
using namespace std;
bool isprime(int n)
{
    if(n==1)
    {
        return false;
    }
    for(int i=2;i*i<n;i++)
    {
        if(n%)
    }
}
int main()
{
    int n;
    printf("Enter the number to check whether entered number is prime or not?");
    scanf("%d",&n);
    isprime(n);
return 0;
}